'use client';

// Re-export the admin recipe editor page component
// This allows accessing recipes via /recipes/[id] instead of /admin/recipes/[id]
export { default } from '../../admin/recipes/[id]/page';
